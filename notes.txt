# NOTES 
### HTML
Learnt about : 
1) img = image, it's self closing 
2) src - source
3) alt = describe your image, when the picture fails to upload it describes the best 
 for example : <img src="https://example_picture.org" alt="example image">
4)A 'a' (anchor) elements to link to content outside of your web page.
a elements need a destination web address called an href attribute. They also need anchor text. Here's an example:
<a href="https://example.org">this links to example</a>
### Day 1
continuing THE BASE PART AGAIN 
5)Anchor tag >> _blank used to open another tab 
6)Href is used for url 
7)Creating an image as a link, nesting it within a anchor tag 
8)Input from a user, self losing tags 
<input type="text">
10)Web forms that actually submit data to a server 
11)Submit button to your form. Clicking this button will send the data from your form to the URL
12)Radio buttons for questions where you want the user to only give you one answer out of multiple options.
#Forgot to write notes here sad but its fine.
14) Div element : is a general purpose container for other elements.
### CSS
#day 3rd Learnt how to import fonts and degrade to another font when one isn't available.
1. Style element made separately at the top so that we can give changes without edititng the body or head
2. Soogle fonts library, copy the font url from the library. 
Google Fonts is a free library of web fonts that you can use in your CSS by referencing the font's URL.
